# Public URL of the backend API.
# Local dev: leave this UNSET — requests use the Vite proxy (/api -> http://localhost:5001).
# Production (Vercel): set this to the backend's public URL, e.g. your ngrok https URL:
#   VITE_API_BASE=https://your-subdomain.ngrok-free.app
VITE_API_BASE=
